#include <stdio.h>
#include <stdlib.h>

int main()
{
	float a, b;
	printf("Enter velocity of the jet (m/s): ");
	scanf("%f", &a);
	printf("\nEnter distance covered by jet (meter): ");
	scanf("%f", &b);
	
}